# Отрисовка QR-кодов при помощи Google Charts

Установка
----------

```
composer install --no-dev
```
--dev - если надо запустить тесты

```
php phpunit.phar --bootstrap vendor/autoload.php tests
```
